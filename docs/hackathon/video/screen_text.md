# Screen text — every overlay, lower-third, watermark and end card

Rule: no overlay claims a metric that has not been measured.

## Persistent watermark (B03–B07)

Bottom-right, mono font, 14 px, 70% opacity:

> `MVP DEMO — public/simulated scenario`

## Lower-thirds (Lucas talking head, B02 only)

> `Lucas Ercolano · Acuífero + Vigía · Gemma 4 Good Hackathon`

## B01 — Hook

Title card (0:02–0:06):

> `ACUÍFERO + VIGÍA`
> `Local flood intelligence when the network fails.`

End of hook:

> `When connectivity fails, flood response cannot wait.`

## B03 — Vigía volunteer

Above input:

> `Volunteer report · Rioplatense Spanish · offline-capable`

Above JSON:

> `Structured local report — schema output`

Tag (do NOT say "accuracy"):

> `Illustrative volunteer report · simulated`

## B04 — Acuífero fixed node

Header:

> `Fixed node · Silverado demo clip`

Side labels:

- `Curated frames T-60 / T-30 / T`
- `Local Gemma runtime`
- `Offline-capable node profile`

Box label on Gemma response:

> `Node assessment package`

Footer tag:

> `Public demo clip · not field data`

## B05 — Connectivity loss

Banner that flashes red on wifi off:

> `NETWORK OFFLINE`

Toast on volunteer report:

> `queued locally`

Toast under fixed-node alert:

> `local alert · no cloud round-trip`

Banner that turns green on wifi back:

> `SYNC COMPLETE`

Caption:

> `Offline-first flow · audible siren on device`

## B06 — Audit trail

Headline strip:

> `Evidence, not just notification.`

Section labels:

- `Reasoning summary (Spanish, Gemma)`
- `Deterministic rule trace`
- `Curated frames`
- `Runner metadata`

## B07 — SINAGIR + impact

Top strip:

> `Export · SINAGIR-compatible alert package`

Map caption:

> `Argentina · Litoral region · demo node markers`

Footer caption:

> `Aligned with Plan Nacional para la Reducción del Riesgo de Desastres 2025–2029`

## B08 — End card

Centered:

```
ACUÍFERO + VIGÍA
Local flood intelligence when the network fails.

MVP today  ·  Public-data evaluation next  ·  Real local data after that

github.com/<org>/AcuIfero4Vigia
Built for the Gemma 4 Good Hackathon — Global Resilience track
```

## Forbidden overlays

These strings must not appear anywhere in the cut:

- `accuracy`, `% accuracy`, `state-of-the-art`, `SOTA`
- `deployed`, `in production`, `live in <city>`
- `predicts X minutes earlier`, `lead time = N`
- `validated on real Litoral data`
- partner / agency logos that have not been signed off
